# Simple C Lib
It is simple implementation of multiple c files and its headers. The build is done using make file and CMake.   
Make sure to use correct relative or absolute path when including a module in your C programme.  
